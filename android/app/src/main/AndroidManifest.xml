<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Runs the ReAct loop as a foreground service so it survives backgrounding. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Used by the Model Manager to fetch .gguf files from Hugging Face,
         and by the agent's own download_resource tool; inference itself
         never touches the network. Both are gated by the Settings
         network-downloads toggle. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="Pagai"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- flutter_background_service_android's own AndroidManifest.xml
             already declares this service (exported=true, matched here so
             there's no merge conflict) but declares no foregroundServiceType
             at all. Without one, Android 14+ throws
             MissingForegroundServiceTypeException the instant the service
             actually calls startForeground() - i.e. the moment the very
             first task is assigned - crashing the whole app. This
             redeclaration exists solely to add that type back. -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:exported="true"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
